name: Build multi-arch RHEL bootc images with native container tools

# Allows for manual builds on any branch
# Will build and push images on release publication via GitHub and pushes to the main branch
# Will build images only on opening a PR, can be extended for testing and gating
on:
  workflow_dispatch:
  push:
    branches: [main]
  release:
    types: [published]
  pull_request:
    branches: [main]

env:
  # Containerfile relative to the repository top level
  CONTAINERFILE: Containerfile
  # Subscription Manager credentials - example uses repository secrets
  RHT_ORGID: ${{ secrets.RHT_ORGID }}
  RHT_ACT_KEY: ${{ secrets.RHT_ACT_KEY }}

  # Source registry for base image
  SOURCE_REGISTRY_HOST: "registry.redhat.io"
  # Source credentials - example uses repository secrets
  SOURCE_REGISTRY_USER: ${{ secrets.SOURCE_REGISTRY_USER }}
  SOURCE_REGISTRY_PASSWORD: ${{ secrets.SOURCE_REGISTRY_PASSWORD }}

  # Destination registry for built images
  # Example is set for pushing to Quay, can target any registry
  # DEST_REGISTRY_HOST: "ghcr.io"
  DEST_REGISTRY_HOST: "quay.io"
  # Destination image namespace and name (eg: myrepo/myimage)
  # DEST_IMAGE: "${{ github.repository }}"
  DEST_IMAGE: "${{ secrets.DEST_REGISTRY_USER }}/${{github.event.repository.name}}"
  # Destination credentials - example uses platform variables and secrets
  # DEST_REGISTRY_USER: ${{ github.actor }}
  # DEST_REGISTRY_PASSWORD: ${{ secrets.GITHUB_TOKEN }}
  DEST_REGISTRY_USER: ${{ secrets.DEST_REGISTRY_USER }}
  DEST_REGISTRY_PASSWORD: ${{ secrets.DEST_REGISTRY_PASSWORD }}

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  compute-tags:
    # Builds the list of image tags to apply based on the trigger:
    # always includes the commit SHA; adds 'latest' only for pushes to
    # main or published GitHub Releases; adds the version tag for
    # releases; adds the sanitized branch name for all other triggers.
    runs-on: ubuntu-24.04
    outputs:
      taglist: ${{ steps.tags.outputs.taglist }}
    steps:
      - name: Compute tag list
        id: tags
        run: |
          set -euo pipefail
          TAGS="${{ github.sha }}"
          REF="${{ github.ref_name }}"
          SAFE_REF="${REF//\//-}"
          EVENT="${{ github.event_name }}"
          if [ "$EVENT" = "release" ]; then
            TAGS="latest $TAGS $SAFE_REF"
          elif [ "$EVENT" = "push" ] && [ "$SAFE_REF" = "main" ]; then
            TAGS="latest $TAGS"
          elif [ "$SAFE_REF" != "main" ]; then
            TAGS="$TAGS $SAFE_REF"
          fi
          echo "taglist=$TAGS" >> $GITHUB_OUTPUT

  build:
    needs: compute-tags
    name: build / ${{ matrix.arch }}
    strategy:
      fail-fast: false
      matrix:
        arch: [amd64, arm64]
        include:
          - arch: amd64
            runner: ubuntu-24.04
          - arch: arm64
            runner: ubuntu-24.04-arm
    runs-on: ${{ matrix.runner }}
    permissions:
      contents: read
      packages: write
    env:
      TAGLIST: ${{ needs.compute-tags.outputs.taglist }}

    steps:
      - uses: actions/checkout@v7

      - name: Install updated container tools
        run: |
          set -euo pipefail
          if [ "$(dpkg --print-architecture)" = "amd64" ]; then
            mirror="http://azure.archive.ubuntu.com/ubuntu"
          else
            mirror="http://ports.ubuntu.com/ubuntu-ports"
          fi
          echo "deb ${mirror} plucky universe main" | sudo tee /etc/apt/sources.list.d/plucky.list
          printf 'Package: *\nPin: release n=plucky\nPin-Priority: 100\n' | sudo tee /etc/apt/preferences.d/plucky.pref
          sudo apt-get update -q
          sudo apt-get install -y --no-install-recommends crun/plucky podman/plucky

      - name: Build image
        run: |
          podman volume create build-storage
          podman run --rm --privileged \
            -v build-storage:/var/lib/containers \
            -v ${{ github.workspace }}:/build \
            -e SMDEV_CONTAINER_OFF=1 \
            -e SOURCE_REGISTRY_HOST \
            -e CONTAINERFILE \
            -e RHT_ORGID -e RHT_ACT_KEY \
            -e SOURCE_REGISTRY_USER -e SOURCE_REGISTRY_PASSWORD \
            -e DEST_REGISTRY_HOST -e DEST_IMAGE \
            registry.access.redhat.com/ubi9/ubi bash -c '
              set -euo pipefail
              dnf -y install buildah
              subscription-manager register --org="${RHT_ORGID}" --activationkey="${RHT_ACT_KEY}"
              trap "subscription-manager unregister" EXIT
              buildah login -u "${SOURCE_REGISTRY_USER}" -p "${SOURCE_REGISTRY_PASSWORD}" "${SOURCE_REGISTRY_HOST}"
              buildah bud -f "/build/${CONTAINERFILE}" -t ${DEST_REGISTRY_HOST}/${DEST_IMAGE}:${{ github.sha }}-${{ matrix.arch }} /build
            '

      - name: Push image
        if: github.event_name != 'pull_request'
        run: |
          STORAGE_ROOT=$(podman volume inspect build-storage --format '{{.Mountpoint}}')
          podman login "${DEST_REGISTRY_HOST}" \
            -u "${DEST_REGISTRY_USER}" -p "${DEST_REGISTRY_PASSWORD}"
          podman --root "${STORAGE_ROOT}/storage" push \
            "${DEST_REGISTRY_HOST}/${DEST_IMAGE}:${{ github.sha }}-${{ matrix.arch }}"

      - name: Cleanup build storage
        if: always()
        run: podman volume rm build-storage

  manifest:
    name: Create multi-arch manifest
    needs: [build, compute-tags]
    if: github.event_name != 'pull_request'
    runs-on: ubuntu-24.04
    permissions:
      packages: write
    env:
      TAGLIST: ${{ needs.compute-tags.outputs.taglist }}

    steps:
      - name: Install updated container tools
        run: |
          set -euo pipefail
          echo "deb http://azure.archive.ubuntu.com/ubuntu plucky universe main" | sudo tee /etc/apt/sources.list.d/plucky.list
          printf 'Package: *\nPin: release n=plucky\nPin-Priority: 100\n' | sudo tee /etc/apt/preferences.d/plucky.pref
          sudo apt-get update -q
          sudo apt-get install -y --no-install-recommends podman/plucky

      - name: Create and push manifest
        run: |
          set -euo pipefail
          IMAGE_REF="${DEST_REGISTRY_HOST}/${DEST_IMAGE}"
          podman login "${DEST_REGISTRY_HOST}" \
            -u "${DEST_REGISTRY_USER}" -p "${DEST_REGISTRY_PASSWORD}"
          podman manifest create localhost/tmp-manifest:build
          for arch in amd64 arm64; do
            podman manifest add localhost/tmp-manifest:build \
              "docker://${IMAGE_REF}:${{ github.sha }}-${arch}"
          done
          for tag in ${TAGLIST}; do
            podman manifest push --all localhost/tmp-manifest:build \
              "${IMAGE_REF}:${tag}"
          done
          podman manifest rm localhost/tmp-manifest:build
